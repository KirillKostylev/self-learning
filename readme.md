To run project you need to preinstall:
* Java 11
* Maven 
* Docker

How to run:
1) Build the project  ```mvn clean install -DskipTests```
2) Run containers ```docker-compose up```

Swagger Open API ```localhost:8080/swagger-ui/index.html```